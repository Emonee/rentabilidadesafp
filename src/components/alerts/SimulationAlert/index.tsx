import BasicAlert from '@/components/alerts/BasicAlert'

export default function () {
  return (
    <BasicAlert message="La simulación es solo una guía y no asegura un resultado exacto a futuro." />
  )
}
